Never commit secrets. Use .env and secret manager.

Validate and sanitize all external inputs.

Keep dependencies updated; fix critical vulns within 48h.

Log authz failures; avoid logging PII.

Threat-model new features (1-pager: entry points, assets, risks, mitigations).
