On PR

Lint → Unit → Integration → Build → Preview

Required to merge: lint, unit, build

Optional but recommended: integration, preview

On Merge to main

Build & push image

Run migrations with --safe

Deploy to staging → canary to prod

Notify #eng-releases with link to changelog
